case $config_platform_branch in
    *-nightly*|*-isengard)
        ;;
    *)
        return
        ;;
esac

rm -f tools/depends/native/TexturePacker/.installed-*-native
eval $config_build_env make -C tools/depends/native/TexturePacker

[ -h tools/depends/native/TexturePacker/bin ] || ln -s ../../armv7l-linux-gnueabihf-native/bin tools/depends/native/TexturePacker/bin
