case $config_source_branch in
  imx6*|rpi*)
    ;;
  *)
    return 0
    ;;
esac

[ -e lib/cpluff/configure.ac ] || return 0
! grep -q AM_GNU_GETTEXT lib/cpluff/configure.ac || sed -i '/AM_GNU_GETTEXT/d' lib/cpluff/configure.ac

## [ -e lib/cpluff/auxliary/config.rpath ] || cp /usr/share/gettext/config.rpath lib/cpluff/auxliary
